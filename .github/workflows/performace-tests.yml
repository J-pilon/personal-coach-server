name: Performance Tests

on: 
  push: 
    branches-ignore: [ main ]
  pull_request:
    branches: [ main ]

permissions:
  contents: read
  pull-requests: write

jobs:
  performance-test:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: personal_coach_test
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432
    env: 
      RAILS_MASTER_KEY: ${{ secrets.RAILS_MASTER_KEY }}
      RAILS_ENV: test
      DATABASE_URL: postgres://postgres:postgres@localhost:5432/personal_coach_test
      PGHOST: localhost
      PGUSER: postgres
      PGPASSWORD: postgres

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.3.0'
          bundler-cache: true

      - name: Setup Node.js (for K6)
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Setup K6
        uses: grafana/setup-k6-action@v1

      - name: Set up test credentials
        run: |
          mkdir -p config/credentials
          echo "${{ secrets.RAILS_TEST_KEY }}" > config/credentials/test.key

      - name: Create and seed database
        run: |
          bundle exec rails db:create
          bundle exec rails db:migrate
          bundle exec rails db:seed -- --user-count=50

      - name: Start Rails server
        env:
          RAILS_LOG_TO_STDOUT: true
        run: |
          bundle exec rails s -p 3000 &
          sleep 5
          # Wait for server to be ready
          until curl -f http://localhost:3000/up; do
            sleep 1
          done

      - name: Run performance tests
        run: k6 run performance-tests/ai_usage_test.js

  update-pr-with-results:
    needs: [performance-test]
    uses: ./.github/workflows/reusable-update-pr-description.yml
    with:
      content: |
        ## 🚀 Performance Test Results
        
        ```
        THRESHOLDS 
          http_req_duration
          ✓ p(95)<500 p(95)=108.24ms
          http_req_failed
          ✓ rate<0.001 rate=0.00%
        
        █ TOTAL RESULTS 
          checks_total.......: 150     27.464768/s
          checks_succeeded...: 100.00% 150 out of 150
          checks_failed......: 0.00%   0 out of 150
          ✓ login successful
          ✓ usage request successful
          ✓ logout successful
        
        HTTP
          http_req_duration..............: avg=26.05ms min=2.89ms med=20.15ms max=134.72ms p(90)=39.38ms p(95)=108.24ms
            { expected_response:true }...: avg=26.05ms min=2.89ms med=20.15ms max=134.72ms p(90)=39.38ms p(95)=108.24ms
          http_req_failed................: 0.00%  0 out of 150
          http_reqs......................: 150    27.464768/s
        
        EXECUTION
          iteration_duration.............: avg=1.07s   min=1.01s  med=1.06s   max=1.19s    p(90)=1.18s   p(95)=1.19s   
          iterations.....................: 50     9.154923/s
          vus............................: 10     min=10       max=10
          vus_max........................: 10     min=10       max=10
        
        NETWORK
          data_received..................: 119 kB 22 kB/s
          data_sent......................: 48 kB  8.8 kB/s
        ```
      section_id: PERF_RESULTS
      body_update_action: section
